<div class="doc-container">
  <!--
  <div class="doc-form">
    <div class="p-auto bg bg-light d-flex flex-column align-items-center justify-content-center">
      <h3>Create Doctor</h3>
      <%= render "form" %>
    </div>
  </div>
    -->
  <div class="container">
    <h2 class="title">Doctors</h2>
    <div class="row">
      <% @doctors.each do |doctor| %>
        <%= link_to doctor_path(doctor), class: "doctor-card" do %>
          <div class="doc-name-img">
            <div>
              <% if doctor.avatar.attached? %>
                <%= cl_image_tag doctor.avatar.key, class: "doc-img"%>
              <% else %>
                <%= image_tag "https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/logo.png", class:"ava-img"%>
              <% end %>
            </div>
            <div class="doctor-label">
              <h4 class="doc-name"><%= doctor.first_name %></h4>
              <h3 class="doc-name"><%= doctor.last_name %></h3>
            </div>
          </div>
          <h5 class="doc-specialty"><%= doctor.specialty %></h5>
          <div class="doc-list-card">
            <ul>
              <li><strong>Hospital:</strong> <%= doctor.hospital %></li>
              <li><strong>Phone Number:</strong> <%= doctor.phone_number %></li>
              <% unless doctor.medical_appointments.last.nil?  %>
                <li><strong>Appointment:</strong> <%= doctor.medical_appointments.last.event.start.strftime("%B %d, %Y at %I:%M %p") %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
      <% end %>
      <!--
      <div class="col-3">
        <div id="map" style="height: 250px; width: 250px" data-multi-mapbox-address-value="<%= @addresses.to_json%>" data-multi-mapbox-target="map" data-multi-mapbox-api-key-value="<%= ENV['MAPBOX_TOKEN'] %>">
        </div>
      </div>
      -->
    </div>
  </div>
</div>
